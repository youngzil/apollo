{
  "name": "apollo-portal-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test:e2e": "playwright test --project=chromium --grep '@smoke|@regression'",
    "test:e2e:ci": "playwright test --project=chromium --grep '@smoke|@regression' --reporter=line,html",
    "test:e2e:auth-matrix": "playwright test --project=chromium --grep '@auth-matrix'",
    "test:e2e:auth-matrix:ci": "playwright test --project=chromium --grep '@auth-matrix' --reporter=line,html"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
